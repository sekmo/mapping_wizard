<%= form_with(model: mapping, local: true) do |form| %>
  <% if mapping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mapping.errors.count, "error") %> prohibited this mapping from being saved:</h2>

      <ul>
      <% mapping.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :partner_id %>
    <%= form.select(:partner_id, options_for_select(Partner.all.map {|partner| [partner.name, partner.id] })) %>
    <%#= form.text_field :partner_id %>
  </div>

  <div class="field">
    <%= form.label :dalia_key %>
    <%= form.text_field :dalia_key %>
  </div>

  <div class="field">
    <%= form.label :dalia_value %>
    <%= form.text_field :dalia_value %>
  </div>

  <div class="field">
    <%= form.label :partner_key %>
    <%= form.text_field :partner_key %>
  </div>

  <div class="field">
    <%= form.label :partner_value %>
    <%= form.text_field :partner_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
